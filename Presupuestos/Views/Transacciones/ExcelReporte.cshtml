@{
    ViewData["Title"] = "Excel";
    var hoy = DateTime.Today;
    var haceUnMes = hoy.AddMonths(-1);
    var mesActual = hoy.Month;
    var anioActual = hoy.Year;
    var mesAnterior = haceUnMes.Month;
    var mesAnteriorAnio = haceUnMes.Year;
    var anioAnterior = anioActual - 1;

}

<partial name="_Submenu" model="SubmenuTransacciones.Excel" />

<h1>Exportar transaciones</h1>

<div class="mb-3">
    <a asp-action="ExportarExcelPorMes" asp-route-mes="@mesActual" asp-route-anio="@anioActual">
        Exportar mes actual - @hoy.ToString("MMMM yyyy")
    </a>
</div>

<div class="mb-3">
    <a asp-action="ExportarExcelPorMes" asp-route-mes="@mesAnterior" asp-route-anio="@mesAnteriorAnio">
        Exportar mes anterior - @haceUnMes.ToString("MMMM yyyy")
    </a>
</div>

<div class="mb-3">
    <a asp-action="ExportarExcelPorAnio" asp-route-mes="@mesActual" asp-route-anio="@anioActual">
        Exportar año actual - @anioActual
    </a>
</div>

<div class="mb-3">
    <a asp-action="ExportarExcelPorAnio" asp-route-mes="@mesActual" asp-route-anio="@anioAnterior">
        Exportar año anterior- @anioAnterior
    </a>
</div>

<div class="mb-3">
    <a asp-action="ExportarTodo" >
        Exportar Todo
    </a>
</div>